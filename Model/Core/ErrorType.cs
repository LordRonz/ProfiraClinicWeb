namespace ProfiraClinic.Models.Core
{
    public enum ErrorType
    {
        UNKNOWN,
        NOT_FOUND,
        FORBIDDEN,
        UNAUTHORIZED,
        WRONG_STRUCTURE,
        VALIDATION_ERROR,
        SERVER_ERROR
    }
}
