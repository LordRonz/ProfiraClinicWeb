namespace ProfiraClinicRME.Infra
{
    public enum ResponseType
    {
        UNKNOWN,
        NOT_FOUND,
        FORBIDDEN,
        UNAUTHORIZED,
        WRONG_STRUCTURE,
        VALIDATION_ERROR,
        SERVER_ERROR        
    }
}
